{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/nginx/public_html/quiz/src/Controller/AccountController.php'|file_link(0) }}">src/Controller/AccountController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/nginx/public_html/quiz/templates/account/index.html.twig'|file_link(0) }}">templates/account/index.html.twig</a></code></li>
    </ul>
</div>

    <div class="products">
        <table>
            <thead>
            <tr>
                <td>Email</td>
                <td>Is Active</td>
            </tr>
            </thead>
            <tbody>
            {% for product in prods %}
                <tr>
                    <td>
                        {{ product.email }}
                    </td>

                    <td>
                        {% if product.isActive %}
                            <strong>Active</strong>
                        {% else %}
                            <strong>Inactive</strong>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>
{% endblock %}
